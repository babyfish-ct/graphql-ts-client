import { Schema } from "relay-compiler/lib/core/Schema";
import { GraphQLTaggedNode } from "relay-runtime";
export declare function parseTaggedNode(schema: Schema, name: string, graphql: string): GraphQLTaggedNode;
